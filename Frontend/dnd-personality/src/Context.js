import React from "react"

export const CreationSettings = {
    raceId: null,
    subraceId: null,
    subracesCount: 0,
    classId: null,
    selection: null};
export const Context = React.createContext();
